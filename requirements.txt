# Python dependencies for the GNN side of the pipeline.
# Tested with Python 3.12. Install PyTorch / PyTorch Geometric following the
# official instructions for your platform and CUDA version first, then:
#   pip install -r requirements.txt

torch>=2.2
torch-geometric>=2.5
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
optuna>=3.5
matplotlib>=3.7
tqdm>=4.66
openpyxl>=3.1          # external_validation.py / xai write .xlsx reports
captum>=0.7            # Integrated Gradients explainability (src/xai)
networkx>=3.0          # SubgraphX graph operations (src/xai/graph_brain)

# Optional: only needed when running the MATLAB SPM pipeline on the fly
# (USE_PRECOMPUTED_SPM = False). Install the version that matches your MATLAB
# release, e.g. from <MATLAB>/extern/engines/python.
# matlabengine
